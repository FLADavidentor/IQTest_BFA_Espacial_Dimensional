import React from 'react';
import { createRoot } from 'react-dom/client';

// Phase 0 placeholder. Real SubtestApp mounts here in Phase 5.
const el = document.getElementById('bfa-subtest-app');
if (el) createRoot(el).render(<div>BFA Espacial — subtest UI (Phase 5)</div>);
